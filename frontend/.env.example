# Local dev — copy to .env.development
VITE_API_URL=http://localhost:5080

# Vercel (serverless proxy in /api) — set in Project → Settings → Environment Variables
# API_PROXY_URL=https://your-dotnet-api.onrender.com
